# Server port
PORT=5000

# Global request timeout (seconds)
REQUEST_TIMEOUT=30

# API Key for authentication (optional, but recommended for production)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# If not set, all requests are allowed (useful for development)
API_KEY=your_generated_api_key_here_replace_with_real_value

# ========================================
# SENSITIVE CREDENTIALS
# ========================================
# Store your secrets here - they will be referenced in ENDPOINTS using ${VAR_NAME} syntax
# This allows you to safely commit ENDPOINTS structure to version control

ENDPOINT_API_KEY=your_endpoint_api_key_here
EXTERNAL_SERVICE_TOKEN=your_service_token_here
DATABASE_PASSWORD=your_password_here
# Add more secrets as needed...

# ========================================
# ENDPOINTS CONFIGURATION
# ========================================
# Must be a JSON array. Supports template variables ${VAR_NAME} for sensitive data.
# You can use simple URL strings or complex JSON objects for each endpoint

# Example 1: Using template variables (RECOMMENDED - separates secrets from structure)
ENDPOINTS='[
  {
    "url": "https://api.example.com/users",
    "method": "POST",
    "headers": {
      "Authorization": "Bearer ${EXTERNAL_SERVICE_TOKEN}",
      "Content-Type": "application/json"
    },
    "json": {"name": "John"},
    "timeout": 30
  },
  {
    "url": "https://db.example.com/query",
    "method": "POST",
    "headers": {
      "X-DB-Password": "${DATABASE_PASSWORD}"
    }
  }
]'

# Example 2: Simple URLs (no sensitive data)
# ENDPOINTS='["http://localhost:5000/task1","http://localhost:5000/task2","http://localhost:5000/task3"]'

# Example 3: Mix simple URLs and complex objects with templates
# ENDPOINTS='["http://localhost:5000/task1",{"url":"https://api.example.com/secure","method":"POST","headers":{"X-API-Key":"${ENDPOINT_API_KEY}"},"json":{"data":"test"}},"http://localhost:5000/task3"]'
